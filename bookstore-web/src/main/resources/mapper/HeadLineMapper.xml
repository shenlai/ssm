<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sl.dao.HeadLineDao">
	<select id="queryHeadLine" resultType="com.sl.entity.HeadLine">
		select * from tb_head_line where enable_status=1 order by priority desc
	</select>
</mapper>